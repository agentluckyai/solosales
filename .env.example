# Minimal env for local development (see README.md)

# Postgres (matches infra/docker-compose.yml)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/solosales?schema=public"

# Redis (matches infra/docker-compose.yml)
REDIS_URL="redis://localhost:6379"

# Optional (SendCloud) — leave empty unless integrating
# SENDCLOUD_API_KEY=
# SENDCLOUD_API_SECRET=
# SENDCLOUD_WEBHOOK_SECRET=
